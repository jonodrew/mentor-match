setup:
  addons:
    - plan: heroku-redis

build:
  docker:
    web: Dockerfile

run:
  web:
    command:
    - gunicorn "app:create_app()"
    image: web
  worker:
    command:
      - celery --app=app.extensions.celery worker --loglevel=info
    image: web
